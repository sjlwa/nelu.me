---
interface Props {
  src: string;
  class?: string;
}

const { class: classList, src } = Astro.props;
---
<div class={`w-full h-full bg-cover bg-center flex overflow-hidden absolute z-0 ${classList}`}
      style={{backgroundImage: `url('${src}')`}}>
  <slot/>
</div>
